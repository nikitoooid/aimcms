= html_content @setting.header if @setting && @setting.header
/ nav class="navbar fixed-top navbar-expand-lg navbar-light bg-light"
/   div class="container-fluid"
    
/     a class="navbar-brand" Aimpulse CMS
    
/     button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false"
/       span class="navbar-toggler-icon"
    
/     div class="collapse navbar-collapse" id="navbarText"
/       ul class="navbar-nav me-auto mb-2 mb-lg-0"
/         - if current_user && current_user.admin?
/           li class="nav-item"
/             = link_to t('admin_panel'), admin_root_path, class: 'nav-link'
/         li class="nav-item"
/           = link_to 'Home', root_path, class: 'nav-link'
/         li class="nav-item"
/           = link_to 'Feature', '#', class: 'nav-link'
/         li class="nav-item"
/           = link_to 'Price', '#', class: 'nav-link'
/       span class="navbar-text"
/         - if user_signed_in?
/           = button_to t('logout'), destroy_user_session_path, method: :delete, class: 'btn btn-link'
/         - else
/           = link_to t('signup'), new_user_registration_path
/           = " | "
/           = link_to t('login'), new_user_session_path
